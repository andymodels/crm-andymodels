{
  "name": "crm-andymodels",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -k -n api,web -c blue,magenta \"PORT=3030 npm run dev --prefix backend\" \"VITE_DEV_PROXY_PORT=3030 npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "puppeteer": "^24.40.0"
  }
}
